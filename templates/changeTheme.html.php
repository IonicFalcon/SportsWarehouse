<section class="changeTheme">
    <form id="adminThemeChange">
        <p class="themeSelect" id="lightMode">
            <input type="radio" name="theme">
            <label for="lightMode">Light Theme</label>
        </p>

        <p class="themeSelect" id="darkMode">
            <input type="radio" name="theme">
            <label for="darkMode">Dark Theme</label>
        </p>

        <p class="themeSelect" id="custom">
            <input type="radio" name="theme">
            <label for="custom">Custom Theme</label>
        </p>

        <fieldset class="customTheme">
            <p>Test</p>
        </fieldset>
    </form>
</section>